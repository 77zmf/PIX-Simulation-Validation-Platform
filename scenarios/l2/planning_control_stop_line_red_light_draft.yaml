scenario_id: stable_l2_planning_control_stop_line_red_light_draft
stack: stable
map_id: Town01
asset_bundle: carla_town01
ego_init:
  pose:
    x: 229.78167724609375
    y: -2.0201120376586914
    z: 0.0
    yaw_deg: 0.0
goal:
  pose:
    x: 314.2434997558594
    y: -1.982629656791687
    z: 0.0
    yaw_deg: -0.03045654296875
traffic_profile:
  mode: stop_line_red_light_draft
  vehicles: 0
  pedestrians: 0
  scripted_agents:
    - red_light_state_controller
weather_profile:
  preset: ClearNoon
sensor_profile: robobus_pixrover14_application_topology
algorithm_profile: planning_control_baseline
seed: 20260426
recording:
  rosbag2:
    enabled: true
    path: rosbags/planning_control_stop_line_red_light_draft
  carla_recorder:
    enabled: true
    path: carla/planning_control_stop_line_red_light_draft.log
kpi_gate: planning_control_stop_line_red_light
labels:
  - L2
  - stable
  - planning_control
  - traffic_light
  - stop_line
  - draft
metadata:
  owner: planning-control
  draft_status: blocked_on_traffic_light_control
  source_assets:
    - docs/runbooks/planning_control_scene_gap_matrix.md
  validation_method: Draft contract only. Promote after CARLA traffic-light state control and stop-line overshoot metric extraction are deterministic.
  validation_command: >
    python3 -c "raise SystemExit('blocked: stop-line/red-light scenario requires deterministic traffic-light state control and stop-line metric probe')"
  expected_observable: Ego stops before the stop line on red, does not enter the conflict zone, then resumes safely when released.
  runtime_gap: No current runtime probe emits red_light_violations or stop_line_overshoot_m; this draft is a KPI contract, not executable acceptance.
  rollback_impact: Keep this draft out of ops/test_campaigns/stable_planning_control_bughunt.yaml until traffic-light control and metrics are real.
execution:
  mode: external
  stable_runtime:
    autoware_ws: /home/pixmoving/zmf_ws/projects/autoware_universe/private_autoware
    autoware_bridge_ws: /home/pixmoving/zmf_ws/projects/autoware_universe/autoware
    ros_rmw_implementation: rmw_cyclonedds_cpp
    autoware_map_path: /home/pixmoving/autoware_map/Town01
    autoware_vehicle_model: robobus
    autoware_sensor_model: robobus_sensor_kit
    carla_root: /home/pixmoving/CARLA_0.9.15
    carla_map: Town01
    carla_vehicle_type: vehicle.pixmoving.robobus
    carla_spawn_point: "229.7817,2.0201,-0.5,0,0,0"
    carla_actor_object_bridge_enabled: "false"
    carla_render_mode: offscreen
    carla_quality_level: Low
